#include <stdio.h>
#include <stdlib.h>
#include "main.h"
/**
 * _strdup - main entry
 * @size: size input inr
 * @c: char
 * Return: 0
 */

char *_strdup(char *str)
{
	int i; end;
	char *array;

	if (str == NULL)
	return (NULL);

	for (end = 0; end <= *str; end++)
	{
		
	}
	end += 1;
	array = malloc(sizeof(char) * end);
	






}

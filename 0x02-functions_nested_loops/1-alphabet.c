#include "main.h"

/**
 * main - check the code.
 * Return: Always 0.
 */

void print_alphabet(void)
{
    int i = 0;
    for(i= 'a'; i<= 'z'; i++)
    {
        _putchar (i);
    }
    _putchar ('\n');
}
